diff --git a/tests/e2e/scene-visual.spec.ts b/tests/e2e/scene-visual.spec.ts
index aeb24e3..36723c8 100644
--- a/tests/e2e/scene-visual.spec.ts
+++ b/tests/e2e/scene-visual.spec.ts
@@ -93,7 +93,7 @@ function runtimeView(view: ReviewView, flipZ: boolean) {
 }
 
 test("@private-assets @scene-visual captures fixed runtime review views", async ({ page, request }) => {
-  test.setTimeout(120_000);
+  test.setTimeout(300_000);
   const assetPath = process.env.SCENE_VISUAL_ASSET_PATH;
   const manifestPath = process.env.SCENE_VISUAL_MANIFEST_PATH;
   const configPath = process.env.SCENE_VISUAL_CONFIG_PATH;
@@ -110,13 +110,23 @@ test("@private-assets @scene-visual captures fixed runtime review views", async
   ]);
   const manifest = JSON.parse(manifestSource) as Record<string, unknown>;
   const config = JSON.parse(configSource) as { reviewViews?: ReviewView[] };
-  expect(config.reviewViews?.length).toBeGreaterThan(0);
+  const requestedViewIds = new Set((process.env.SCENE_VISUAL_VIEW_IDS ?? "").split(",").filter(Boolean));
+  const reviewViews = requestedViewIds.size > 0
+    ? (config.reviewViews ?? []).filter((view) => requestedViewIds.has(view.id))
+    : (config.reviewViews ?? []);
+  expect(reviewViews.length).toBeGreaterThan(0);
+  if (requestedViewIds.size > 0) expect(reviewViews.length).toBe(requestedViewIds.size);
   manifest.glbPath = "scene.glb";
   manifest.renderProfile = "baked-pbr-v1";
   delete manifest.preview;
   if (process.env.SCENE_VISUAL_STRIP_ANCHORS === "1") {
     delete manifest.anchors;
   }
+  if (process.env.SCENE_VISUAL_HIDE_MEDIA_SURFACES === "1" && Array.isArray(manifest.mediaSurfaces)) {
+    manifest.mediaSurfaces = manifest.mediaSurfaces.map((surface) => (
+      typeof surface === "object" && surface !== null ? { ...surface, visible: false } : surface
+    ));
+  }
 
   const bundleId = `scene-visual-${Date.now()}`;
   const uploadResponse = await request.post("/api/scene-bundles/uploads", {
@@ -150,7 +160,11 @@ test("@private-assets @scene-visual captures fixed runtime review views", async
       sceneBundleUrl: ["127.0.0.1", "localhost"].includes(new URL(uploaded.publicUrl).hostname)
         ? new URL(new URL(uploaded.publicUrl).pathname, baseUrl).toString()
         : uploaded.publicUrl,
-      avatarConfig: { avatarsEnabled: false }
+      avatarConfig: {
+        avatarsEnabled: true,
+        avatarFallbackCapsulesEnabled: false,
+        avatarSeatsEnabled: false
+      }
     }
   });
   expect(roomResponse.ok()).toBeTruthy();
@@ -185,7 +199,8 @@ test("@private-assets @scene-visual captures fixed runtime review views", async
 
   const flipZ = process.env.SCENE_VISUAL_FLIP_Z === "1";
   await mkdir(resolve(outputDir!), { recursive: true });
-  for (const view of config.reviewViews ?? []) {
+  await page.addStyleTag({ content: ".hud { display: none !important; }" });
+  for (const view of reviewViews) {
     const pose = runtimeView(view, flipZ);
     const applied = await page.evaluate((input) => (
       window as Window & {
@@ -196,8 +211,7 @@ test("@private-assets @scene-visual captures fixed runtime review views", async
     await page.evaluate(() => new Promise<void>((resolveFrame) => {
       requestAnimationFrame(() => requestAnimationFrame(() => resolveFrame()));
     }));
-    const dataUrl = await page.locator("#scene > canvas").evaluate((canvas: HTMLCanvasElement) => canvas.toDataURL("image/png"));
-    await writeFile(resolve(outputDir!, `${view.id}.png`), Buffer.from(dataUrl.split(",")[1] ?? "", "base64"));
+    await page.screenshot({ path: resolve(outputDir!, `${view.id}.png`), type: "png" });
   }
 
   const diagnostics = await page.evaluate(() => (
